apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.preferredrefreshrate'
version = '1.0.0'

android {
  namespace "expo.modules.preferredrefreshrate"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
}
