# 📸 Estado actual de Manifiesto Mobile — 2026-05-21

> Foto completa del proyecto verificada contra el código real en commit `7962ea2` (rama `main`).
> Generado por exploración directa del código vía 7 agentes paralelos + síntesis.
>
> ⚠️ **Nota (2026-05-22):** foto al commit `7962ea2`, **antes** de la limpieza del doc 09. El 2026-05-22 se ejecutó esa limpieza (156 archivos eliminados) y se reorganizó la documentación bajo `docs/`. Varios archivos de código citados aquí (los listados en [09](09-candidatos-a-eliminar.md)) ya no existen — esperable en una foto fechada.
>
> 🆕 **Novedad post-foto (2026-05-22):** se cerró la vuln RLS de `expenses` (migración `20260522000000`) y se implementó el **modo soltero / familia invisible** (`families.kind`, onboarding Solo/Familia, UI de familia oculta en Home/Settings). Doc canónico: [sistemas/account-kinds.md](../../sistemas/account-kinds.md). Esta foto no refleja esos cambios.
>
> 🆕 **Novedad post-foto (2026-05-27):** **Pre-onboarding biometric setup** — toda cuenta nueva pasa por una pantalla intermedia `/(app)/biometric-setup` (entre signup y wizard) para activar Face ID conscientemente. Controlada por `AppEntryGate` con flag per-user en SecureStore; cubre email+password, Apple, Google y magic-link confirm. Detalle en [02 § "Pre-onboarding biometric setup"](02-auth-onboarding.md#pre-onboarding-biometric-setup--live-2026-05-27).
>
> 🆕 **Novedad post-foto (2026-05-27, parte 2):** **Tour-seen backend sync** — el estado "tour visto" se movió de SecureStore device-local a 4 columnas timestamptz en `profiles` (`home_tour_seen_at`, `gastos_tour_seen_at`, `fijos_tour_seen_at`, `control_tour_seen_at`) + 3 RPCs (`mark_tour_seen`, `reset_tour_seen`, `reset_all_tours_seen`). Logout deja de borrar el estado (backend persiste cross-session/device); migración one-shot por device hoistea flags legacy al backend. Resuelve el bug "después de logout veo el tour de nuevo". Detalle en [06 § "Tours / Walkthroughs"](06-settings-engagement.md#7-tours--walkthroughs).
>
> 🆕 **Novedad post-foto (2026-05-29) — rama `feat/settings-dark-mode`:** paquete grande de modo oscuro + engagement, mergeado a `main`. Incluye: **modo oscuro near-black** (`#0A0F0C`) en todo el cluster Ajustes (cards en `surfaceMuted`); **lenguaje de celebración de logros** nuevo (`AuroraBloom` + anillo que se dibuja `DrawRing` + pop spring) y **racha** con dial de llama centrado, todo con **tonos de tier dark-aware**; **Plan del Hogar** con detalle por plan (highlights, ahorro anual); **bloque derivado de plan de ahorro** en la config de Meta; **grid completo de avatares** navegable con "Guardar avatar" fijo; **Notificaciones V2** (minificado, sin filtros, ver + marcar leída con hard-delete, salida más suave); **empty states reales** en Fijos y Gastos (componentes reales en modo placeholder, sin data falsa) y **empty-state por card en Control** con condiciones de activación reales (`closedDays`/`diasConGasto`); borde dark unificado en cards de Home; copy neutralizado (sin voseo); y migración `20260528120000` que repara la policy RLS del catálogo de logros. Detalle en [02](02-auth-onboarding.md), [03](03-home-control-fijos.md), [04](04-gastos-add-flows.md), [06](06-settings-engagement.md) y [07](07-backend-servicios-db.md).
>
> 🆕 **Novedad post-foto (2026-05-31, parte 8m) — copy del marquee disambigua del banner:** el eyebrow de la card del marquee horizontal pasó de "PRÓXIMOS A PAGAR" a **"POR PAGAR · ESTE CICLO"** para diferenciarlo claramente del `FijosScheduledBanner` ("N fijos programados · Sin pagar todavía"). Convivían dos surfaces sobre fijos no pagados con copy ambiguo (¿qué es "próximo"? ¿el de mañana o el del trimestre que viene?). Ahora el marquee comunica "lo que tenés que pagar AHORA (ciclo activo)" y el banner comunica "fijos creados pero todavía no entran al ciclo" — mental models distintos, copy distinto.
>
> 🆕 **Novedad post-foto (2026-05-31, parte 8l) — 3 tabs + FijosScheduledBanner contextual:** decisión tras investigar el uso real de la tab "Próximos" post-fix de cycle-coverage (parte 8k abajo): los `future` items quedaron como un caso del ~1% del tiempo y la tab estaba casi siempre vacía. Comparable analysis (Bills iOS, Mint, YNAB, PocketGuard, Apple Wallet) confirmó que **ninguna app de referencia tiene tab dedicada para "future"**; el patrón es lista cronológica unificada o surface contextual aparte. **Decisión Opt A**: tabs vuelven a **3 buckets** (Vencidos / Pendientes / Pagados) y los future items se mueven a un **nuevo `FijosScheduledBanner`** — card sky-muted (info, no urgencia), collapsible, que vive arriba del `FijoCategoryGroups` y aparece en todas las tabs. Renderea `null` cuando no hay future items (99% del tiempo). Collapsed: icon `event` + "N fijos programados" + sub "Sin pagar todavía" + chevron animado (rotation 180°, ReduceMotion-aware). Expanded: lista con `catColor` dot + name + "Vence DD MMM". Hairline border + radius 12 (iOS look). Bg sky-muted matchea el card del padre. Reemplaza la cuarta tab sin perder visibilidad del calendario lejano.
>
> 🆕 **Novedad post-foto (2026-05-31, parte 8k) — fix v4 cycle-covered-by-prior-payment:** caso reportado por el user en `kontosmario@gmail.com` (Claude AI, monthly, pagado anticipado): `next_due_on` cayó **1 día después** del `cycleEnd` → la lógica v3 lo clasificaba como `future`, pero el user lo entendía como "ya pagué, está cerrado este ciclo" → fricción cognitiva. Fix: `computeItemStatus` ahora detecta el caso `next_due_on >= cycleEnd Y last_paid_at != null` → status `'paid'` (NO future). Solo cae a `'future'` si **nunca** se pagó. Helper nuevo `previousCuotaPeriodMonth(item)` deriva el `cuotaMonth` para paid-via-coverage cases (sustrae 1 unidad de la frecuencia al `next_due_on` actual — ej Claude `next_due_on=jun/21` monthly → cuota cubierta = `may/21` → `cuotaMonth = may`). El row sigue mostrando "Cuota de mayo · pagada" con el mes correcto. **Data fix puntual**: Gym (trimestral, día 4) tenía `next_due_on = 2026-09-04` (un trimestre adelantado) por arrastre del bug viejo del form de alta — corregido a `2026-08-04`.
>
> 🆕 **Novedad post-foto (2026-05-31, parte 8j) — card "Por pagar · este ciclo" como marquee horizontal:** rediseño completo de `FijosProximosCard`. **Antes**: lista vertical de 3 rows (alta, repetitiva, ocupaba ~280pt). **Ahora**: marquee horizontal auto-scrolling, ticket-style, single-row layout (`●name + amount + timing pill`), width 240pt c/u, hairline border, radius 11, iOS list-cell feel. Items duplicados internamente para loop seamless. Iteraciones de animación: primero `withRepeat(withTiming(...))` (tenía bug de "salto" por mismatch entre `fullWidth/2` y `setWidth` real cuando el layout final difería del estimado) → final con **`useFrameCallback` + modulo en UI thread** (100% fluido, no acumula drift). Edge fade gradients **removidas** (generaban "sombra gris" en dark mode); patrón iOS = clean cut at boundary. Gesture: `Gesture.Pan` para arrastrar lateral; al soltar la animación **CONTINÚA desde la nueva posición** (no salta a 0) vía `setActive(false)` durante drag + `setActive(true)` en `onEnd`. Speed: 35 px/seg lineal. Header con **`UrgentHeaderDot`** de 7pt al lado del eyebrow que pulsa cuando hay items con vencimiento ≤2 días. Componentes internos: `UpcomingMarquee` (orquesta loop+gesture+frame), `MarqueeTicket` (la celda 240pt), `UrgentHeaderDot`. Pre-iteraciones rápidamente descartadas: versión "premium" con shadows + edge fades + double-row layout (revertidas tras feedback "look enterprise, no iOS").
>
> 🆕 **Novedad post-foto (2026-05-31, parte 8i) — pulse focalizado al chip VENCIDOS + pull-to-refresh:** el `urgencyRing` del `FijosHeroCard` envolvía toda la card con un border peach pulsante (overlay absolute, alpha 0.12→0.42, 2.4s) — invasivo, todo el bloque latía y competía con el contenido. **Movido al badge "X VENCIDOS"** del header del card: pulse del badge (bg alpha 0.18→0.36, border 0.50→0.85, scale 1→1.04, 2.4s warm, ReduceMotion-aware). El pulse vive sobre el componente que ya canalizaba la urgencia (el chip rojo), no sobre el chrome de toda la card. **Pull-to-refresh** agregado a `FijosV2Screen` y `ControlV2Screen` — `RefreshControl` con `tintColor`/`colors` brand, dispara `useHomeSnapshot.refetch()` que re-seedea **todos los caches del cluster** (no solo el de la pantalla). Cierra el último gap entre realtime y "el user quiere forzar update ahora".
>
> 🆕 **Novedad post-foto (2026-05-31, partes 8h₁-8h₅) — refinamientos visuales del FijoRow:** múltiples iteraciones de UX/visuales del row en respuesta a feedback granular del user, terminadas hoy. **(1) Sub-line stackeada en 2 lanes**: el row pasó de 1 lane congestionada (name + amount + meta + pay-btn) a layout en 2 strips: **top** (name + amount + pay-btn) y **bottom-strip** (month chip + status badge). Entra todo sin elipsis. **(2) Mes como chip pill** ("Mayo" / "Junio") con bg+border tintados por status accent, en vez de inline-text. **(3) Status badge inline** con icon + texto bold corto: `overdue` "Vencida 11d" (warning), `pending` "En 5d" / "Hoy" (schedule), `paid` "Pagada" (check-circle), `future` "Próxima" (event). Antes era "En mora · 11d" multi-line; ahora todo single-line dentro del pill. **(4) Status accent en el expand panel**: border-top dashed + bg del statsHero + accent stripe vertical tintados por status (overdue rojo, pending peach, paid lime, future sky). **(5) Botón Pagar refinado**: removido el inner highlight blanco tras feedback "se ve plástico", quedó flat con borde más oscuro.
>
> 🆕 **Novedad post-foto (2026-05-30, parte 8g) — botón "Pagar" inline con identidad visual:** El botón circular junto al monto en cada FijoRow pasó de un check genérico (negro/rojo) a un botón brand-coded con ícono `attach-money` ($). Pending: fondo forest-deep (verde brand) + ícono blanco. Overdue: fondo red brand + halo pulse continuo (1.5s, scale 1→1.45, opacity 0.45→0, infinite, ReduceMotion-aware) que "respira" pidiendo atención sin invadir. Inner highlight 1px alpha blanco (lift sutil) + borde 1.5pt en tono más oscuro (finish curado, no flat-render). 40pt visual + hitSlop 8 = ~56pt efectivo. Extraído a un componente standalone `InlinePayButton` para encapsular la lógica del pulse.
>
> 🆕 **Novedad post-foto (2026-05-30, parte 8f) — fix clasificación de "Vencidos":** `computeItemStatus` clasificaba como `pending` los fijos cuyo `next_due_on` caía DENTRO del ciclo activo, sin chequear si ese día ya había pasado. Resultado: un fijo con vencimiento el día 22 visto un día 30 (mismo mes / mismo ciclo) aparecía en "Pendientes" cuando ya estaba **vencido en la vida real**. Fix: ahora compara `next_due_on < today` para overdue (cubre tanto mora arrastrada de ciclos previos como cuotas del ciclo activo cuyo día ya pasó). El gating `>= cycleEnd` para `future` se mantiene (sigue resolviendo el bug de recurrencia de trimestrales recién pagados). Migración no requerida.
>
> 🆕 **Novedad post-foto (2026-05-30, parte 8e) — expand panel enriquecido del FijoRow:** El panel que se abre al tocar un fijo pasó de un grid técnico de 4 tiles (Frecuencia / Kind / Próx. vencimiento / Categoría) a 4 secciones jerárquicas pensadas para un usuario no-financiero: (1) **Stats hero** "SE LLEVA AL AÑO $3.336.000" + "8% de tu sueldo mensual" (cifra grande tabular, gancho educativo principal — pone el costo en perspectiva anual que la cuota mensual oculta); (2) **Tendencia** con sparkline + copy humano "Subió 12% desde el último pago" + sub con contexto del rango "+18% en 4 pagos"; (3) **Este pago** con InfoLines: frecuencia, próximo vencimiento humanizado ("Vence el 10 de junio (en 5 días)" / "Venció hace 3 días"), categoría; (4) **Historial** "X cuotas registradas · Ya pagaste $Y en total". Aggregates: 4 campos nuevos en `FijoItem` (`annualCost`, `pctOfIncome`, `paymentsLifetime`, `totalPaidLifetime`). Sin migración — todo cliente. Detalle en [03](03-home-control-fijos.md).
>
> 🆕 **Novedad post-foto (2026-05-30, parte 8d) — fix UX al CREAR fijo (toggle "Ya pagué la cuota más reciente"):** El form de alta de un fijo dejó de generar "salto de mes" cuando el user marcaba un fijo como ya pagado. Antes: `buildNextDueOn` apuntaba al próximo `day_of_month` futuro; tocar "Registrar pago" enseguida avanzaba `next_due_on` un mes más → la cuota recién marcada quedaba skipeada. Bug confirmado en prod con `kontosmario@gmail.com` (11 fijos desfasados — Apple marito, Cochera, Seguro del auto, Apple aye, Netflix, Disney+, Ecogas, Compras del mes, Spotify, Apple apps, Sodero). Data fix puntual aplicado (rollback de `next_due_on` 1 mes en los 11). Ahora: `buildNextDueOn` siempre apunta a la cuota del MES ACTUAL (clampada al último día válido) y un toggle nuevo en step 2 del wizard "Ya pagué la cuota más reciente" (default OFF, solo creación, no installments) encadena `recordFixedExpensePayment` tras el create — la RPC inserta el payment row + avanza `next_due_on` al mes siguiente. `createFixedExpense` + `useCreateFixedExpense` ahora devuelven `{ id }` para chainear el RPC sin round-trip extra. Migración no requerida — todo cliente. Detalle en [03](03-home-control-fijos.md).
>
> 🆕 **Novedad post-foto (2026-05-30, parte 8c) — fijos code-complete: 4 tabs + label de mes + revertir pago:** Cierre del paquete grande de Gastos Fijos. **Tabs**: pasaron de 3 a **4 buckets** (Vencidos / Pendientes / Pagados / Próximos) — separar "Vencidos" hace la mora prominente con color rojo brand-deep, en vez de mezclarse con los pendientes del ciclo. Auto-promote a "Vencidos" en primer paint si hay vencidos (respeta selección manual luego). **Label de mes** en cada row: "Cuota de junio · pagada", "Cuota de mayo · en mora 12d", "Próxima · cuota de julio". El usuario sabe exactamente a qué cuota corresponde el row sin ambigüedad. **Revertir pago**: nuevo botón "Revertir pago" en el expand panel de los rows `paid` + snackbar "Pago de X registrado · Deshacer" durante 5s tras cada pago exitoso. Backend: nueva RPC `revert_fixed_expense_payment(uuid)` que hace rollback atómico (borra expense + payment row + retrocede next_due_on + restaura last_paid_at + ajusta installments_paid/remaining_balance + vuelve status a active si estaba completed). Nueva columna `fixed_expense_payments.expense_id uuid` linkea payment↔expense para que el revert sea atómico — backfill incluido para rows existentes via timestamp proximity (±2s). Migración `20260530180000` aplicada a prod 2026-05-30. Hook `useRevertFixedExpensePayment` con optimistic update + rollback en error. Detalle en [03](03-home-control-fijos.md) y [07](07-backend-servicios-db.md).
>
> 🆕 **Novedad post-foto (2026-05-30, parte 8) — recurrencia respetada + sheet de confirmación de precio en fijos:** Dos features atadas en el cluster Gastos Fijos. **(1) Recurrencia respetada**: el listado principal ya no muestra fijos que no tocan este ciclo. Antes un trimestral pagado en abril seguía apareciendo como "pendiente" en mayo y junio (`computeItemStatus` solo comparaba `next_due_on < today`). Ahora el estado del item es cycle-aware (`paid` · `pending` · `overdue` · `future`) comparando contra `[cycleStart, cycleEnd)`. Los `future` (al día con próximo en un ciclo futuro) se relegan al tab "Pagados / Próximos"; los `overdue` (mora arrastrada de ciclos previos) se quedan visibles en "Pendientes" con badge rojo "En mora · Nd" para no perderlos. Tabs simplificadas a 2 buckets (eliminamos "Todos" y "Zombis", esta última ya estaba deprecada). El `total` del summary excluye los `future` para que el ring y "% del sueldo" reflejen solo el costo del ciclo activo. **(2) Sheet de confirmación de precio**: nuevo `ConfirmFixedPaymentSheet` que se abre al tocar "✓ Registrar pago" en el 2do+ pago de un fijo (el 1ero va directo, sin sheet). Header con nombre + último monto registrado + chip "Cobrado con mora" cuando el fijo está overdue. Dos modos: "Mismo · $X" (primary, default) o "Cambió" (revela input precargado con el último monto). Preview inline del delta ("+$1.500 · +12%") antes de confirmar. **Backend**: migración `20260530120000_record_fixed_payment_amount_override.sql` reemplaza el RPC `record_fixed_expense_payment` para aceptar `p_amount_override numeric DEFAULT NULL` — cuando vino del sheet con monto editado, lo usa como `price` del expense Y lo persiste como nuevo `amount` base del commitment. Agrega columna `expenses.paid_in_arrears boolean` que el RPC setea según `next_due_on < current_date` (snapshot ANTES de avanzar `next_due_on`). La UI usa el flag para distinguir el chip **"Aumento de precio"** (delta positivo en pago normal) del **"Incremento con intereses · int."** (delta positivo + último pago fue cobrado con mora — visible explicitando que la suba puede explicarse por punitorios del servicio). `home_snapshot` y `gastos_expenses_paginated` re-proyectados para incluir `paid_in_arrears` en el bundle. Sin tablas nuevas, sin tasa de interés persistida. **Migración aplicada a prod 2026-05-30** junto con backfill de 4 migraciones atrasadas (`20260529120000`/`130000`/`140000`/`150000` — estaban en git pero pendientes de deploy remoto). **Parte 8b — UX refinements (mismo día)**: tabs pasaron de 2 → 3 (Pendientes / Pagados / Próximos — separa los `future` aparte del cerrado del ciclo, color sky-muted), y el botón **"Registrar pago"** salió del expand panel del row y ahora vive inline en la card collapsed como circular junto al monto (visible siempre para pending/overdue, hitSlop 8px sobre 36pt → ~52pt efectivo, press scale 0.92). La acción más frecuente está a 1 tap, no 2. Detalle en [03](03-home-control-fijos.md) y [07](07-backend-servicios-db.md). Spec en [docs/superpowers/specs/2026-05-30-fijos-recurrencia-y-precio-design.md](../../superpowers/specs/2026-05-30-fijos-recurrencia-y-precio-design.md).
>
> 🆕 **Novedad post-foto (2026-05-29, parte 7) — rebuild del componente de swipe + polish cross-surface:** Después de la integración inicial en fijos (parte 6, abajo), el componente `SwipeableRow` viejo (basado en `ReanimatedSwipeable` de gesture-handler) acumuló bugs visuales (corner gaps, halos en press por el bg-behind, doble-wrap en fijos). Lo **reemplacé desde cero** por **`mobile/components/ui/swipe-row.tsx`** (`SwipeRow`), custom con `Gesture.Pan` v2 + Reanimated v3 worklets en UI thread. Diseño nuevo: action panels **off-screen en idle** (no detrás del row), trasladados en sync con el row vía `useAnimatedStyle` que lee el mismo `translateX` — sin bg-behind, sin halos en press, sin huecos en esquinas redondeadas. Gesture continuation correcta (`startX` + `startTranslation` capturados en `onStart`, `cancelAnimation` en `onBegin` para hacerlo interrumpible — emil), spring único `{damping:22, stiffness:200, mass:0.85}` simétrico para abrir/cerrar, velocidad del finger preservada en `withSpring` (Apple-style), snap-to-nearest 50% con velocity-aware flick, haptic SOLO en transición cerrado→abierto. Polish adicional: chrome unificado cross-surface — los rows de `NotificationRow` y `FijoRow.card` cambiaron a `borderTopLeftRadius/borderBottomLeftRadius` (solo izquierda) matcheando el patrón de `GastoRow`/`ActivityRowV2`; el SwipeRow exterior provee el contorno redondeado completo y el lado derecho del row queda flush con el panel de acción (sin gap). `FijoRowPlaceholder` (standalone, fuera de SwipeRow) restaura las 4 esquinas vía `styles.placeholderCard`. `ActivityRowV2` ahora usa `surfaceMuted` en dark / `creamCard` en light (theme-aware igual que `GastoRow`) — antes era `creamCard` fijo, se leía más claro que la lista de Gastos. El `SwipeableRow` viejo fue eliminado. Aplicado consistente en: Home actividad, Gastos · Movimientos, Fijos, Notificaciones. Detalle en [03](03-home-control-fijos.md).
>
> 🆕 **Novedad post-foto (2026-05-29, parte 6) — rama `feat/swipeable-fijos`:** **Swipe-to-action en gastos fijos** (integración inicial — superada por la parte 7 que reescribió el componente). `FijoRow` envuelto en el componente de swipe compartido con Home actividad, Gastos · Movimientos y Notificaciones — acción única "Eliminar" (danger). Las acciones del row (tap-expand → pagar / editar) quedan intactas; el swipe es atajo destructivo. "Ajustado para cada vista" (notif = neutral "Listo" mark-read · resto = danger "Eliminar").
>
> 🆕 **Novedad post-foto (2026-05-29, parte 5) — rama `feat/income-delete-and-notifs`:** **Ingresos completos.** (1) **Borrar ingreso** desde el swipe del row en Home actividad y en Gastos · Movimientos, mismo patrón que los gastos: `useDeleteIncomeEvent` con optimistic + syncAll + retry toast. (2) **Notificación cuando entra plata**: trigger DB `trg_income_notification` (migración `20260529150000`) emite una notif kind `income_logged` (severity success) al feed compartido; `useCreateIncomeEvent` además dispara push a la familia. (3) **Grupo nuevo "Ingresos"** en `utils/notifications` (label, icono `💵`, mapeo de kind) + entrada en Ajustes → Notificaciones para mutearlo aparte. Migración aplicada a prod. Detalle en [03 § "Home actividad"](03-home-control-fijos.md) y [07 § "Triggers"](07-backend-servicios-db.md).
>
> 🆕 **Novedad post-foto (2026-05-29, parte 4) — rama `feat/income-events-in-feed`:** **Ingresos visibles en la actividad.** Hasta ahora los income events (`income_events`) solo sumaban al total del ciclo; no se veían en ningún lado. Ahora se intercalan con los gastos en el feed por timestamp desc en **Home actividad** y en **Gastos → Movimientos** (mismo día como bucket; días con solo ingreso generan sección propia). Visualmente distintos: monto positivo en verde, ícono por kind (`💸` transferencia · `⭐` bono · `🎁` regalo · `💵` otro), category "Ingreso · X". Hook nuevo `useIncomeEvents` (cap 100 por familia, key existente, ya wired al optimistic create). `syncAll` ahora invalida la income list cuando se mueve algún expense para que ambos refetcheen juntos. Sin cambios de DB. Detalle en [03 § "Home actividad"](03-home-control-fijos.md) y [04 § "Gastos · Movimientos"](04-gastos-add-flows.md).
>
> 🆕 **Novedad post-foto (2026-05-29, parte 3) — rama `feat/state-sync-coherence`:** **Coherencia de estado cross-screen.** Toda mutación core ahora hace **optimistic update** (UI instantánea) + invalida todo el downstream vía un helper centralizado `syncAllAfterMutation` scope-based (`expenses | fixed | fixedPayment | income | savings | notifications | wrapped`), incluyendo los **snapshot roots** (resuelve el clobbering estructural). Si una mutación falla, **rollback silencioso + toast con "Reintentar"** (toast bus nuevo en `lib/toast-bus.ts` + `<ToastHost />` montado en el app-shell). Aplicado en: create/update/delete expense, las 5 fixed (create/update/updateStatus/recordPayment/delete), savings goal, income event, delete notification(s), mark cycle wrapped seen. Resultado: agregar un gasto se ve al instante en Home actividad + Gastos + Control + Fijos sin reload. Detalle en [01 § "Estado de servidor"](01-arquitectura-stack-navegacion-estado.md#estado-de-servidor-react-query--supabase). Spec: [2026-05-29-state-sync-design.md](../../superpowers/specs/2026-05-29-state-sync-design.md).
>
> 🆕 **Novedad post-foto (2026-05-29, parte 2) — rama `fix/monthly-rollup-cycle-window`:** **Control "VS mes" + cierre de ciclo.** (1) **Fix del rollup**: `try_close_previous_cycle` calculaba mal el ciclo previo cuando `salary_payment_day != 1` (ventana de ancho cero → `monthly_summaries` con total 0 → la card "VS mes" nunca activaba). Migración `20260529120000` + backfill de las 7 familias afectadas. (2) **Card rediseñada** a **"CÓMO VAS ESTE MES"**: minimal, comparación-first, **siempre data real** (sin demo); estados Recién arranca / Primeros días / Vas bien-Ojo; dos barras animadas (mes pasado vs este mes) + mini-recap del cierre. (3) **Acceso al "Manifiesto Wrapped"** (animación de cierre) desde Control: botón con ícono `slideshow` + halo sonar en el header (aparece solo si hay un cierre **no visto**, desaparece al verlo) y CTA "Ver el cierre de {mes}" en la card. (4) **Estado visto/no-visto** en `monthly_summaries.wrapped_seen_at` + RPC `mark_cycle_wrapped_seen` (migración `20260529130000`); `home_snapshot()` ahora devuelve `wrapped_seen_at` + `top_expense` (migración `20260529140000`) para que el flag no se pierda al re-sembrar el cache. Detalle en [03](03-home-control-fijos.md) y [07](07-backend-servicios-db.md).

---

## Cómo leer este snapshot

Cada documento cubre un dominio del producto, con:
- **Narrativa** de cada vista/screen y cada sistema/servicio.
- **Tablas-inventario** que catalogan todos los componentes, features, hooks, libs y migraciones del dominio.
- **Estado** de cada pieza (✅ LIVE / 🟡 PARCIAL / 🔴 NO EXISTE / ⏸️ EN PAUSA / ⛔ BLOQUEADO).
- **Referencias** clicables a archivos reales.

---

## 📚 Documentos

| # | Documento | Cubre |
|---|-----------|-------|
| 01 | [Arquitectura, stack, navegación, estado y theme](01-arquitectura-stack-navegacion-estado.md) | Stack técnico, dependencias, expo-router layouts, navegación custom, theme system, providers, query-client, runtime/lib infra, stores de estado, hooks globales |
| 02 | [Auth y Onboarding](02-auth-onboarding.md) | Welcome, login, signup, join, forgot/reset password, callback OAuth, Apple/Google Sign-In, biometría, **pre-onboarding biometric-setup gate** (2026-05-27), onboarding wizard, household setup, account deletion UI |
| 03 | [Home, Control y Fijos](03-home-control-fijos.md) | Home screen, Control v2 (+ hero), gastos fijos (fijos v2/v3, alta de fijo), todos los componentes de home |
| 04 | [Gastos y flujos de alta](04-gastos-add-flows.md) | Lista de gastos / gastos v2, alta de gasto e ingreso, ciclos cerrados, edición de gasto, categorías, notas |
| 05 | [Insights, Asistente y Coach](05-insights-asistente-coach.md) | Motor de señales, asistente financiero heurístico, coach mode, achievements, streaks, finance |
| 06 | [Settings y sistemas de engagement](06-settings-engagement.md) | Settings stack completo, family-admin, plan/billing, notificaciones, galería de logros, ediciones, Wrapped, dev tools, subscriptions-zombie, tours, bridges |
| 07 | [Backend, servicios y base de datos](07-backend-servicios-db.md) | Edge functions, esquema DB (107 migraciones), RPCs, RLS, triggers, crons, telemetría, push, CI/CD, tests, scripts |
| 08 | [Estado vs decisiones pasadas](08-estado-vs-decisiones-pasadas.md) | Dónde estamos hoy respecto a cada decisión del owner (audit, buckets, skips, deferrals, bloqueos Apple) |
| 09 | [Candidatos a eliminar](09-candidatos-a-eliminar.md) | Dead code, deprecado y dead notes verificados con grep: ~87 archivos de código + ~20 docs + `legacy-web-src/` + cruft local. ✅ Ejecutado 2026-05-22 (156 archivos eliminados) |

---

## ⚡ Resumen ejecutivo

**Producto:** app de finanzas familiares compartidas (es-AR). **Feature-complete** desde 2026-05-09; foco actual en operación, hardening, performance y pulido.

**Stack** ([01](01-arquitectura-stack-navegacion-estado.md)): Expo 54 + React Native 0.81.5 (New Architecture) + expo-router + Supabase (Postgres/RLS/RPC/Edge/cron/realtime). React Query v5 para server state (staleTime 30s, gcTime 24h, sin persistir datos financieros), pub/sub módulo-level para UI efímera, Reanimated v4 para animación. 3 stacks anidados (Root → AppStack → Tabs); tab bar **custom** con BlurView Liquid Glass + `lazy:false`/`animation:'none'` (refactor de nav del 2026-05-13, LIVE).

**Vistas — qué está LIVE vs no:**
- ✅ Auth completo (email/pass LIVE; Apple/Google 🟡 wired pero bloqueados por Apple Dev), onboarding wizard 5 pasos, account deletion end-to-end ([02](02-auth-onboarding.md)).
- ✅ Home + Control v2 + **FijosV2** (FijosV3 revertida). `fijos-hero-preview` (41 archivos) y variantes B-G de `control-hero-preview` fueron **eliminados el 2026-05-22** (Bucket 1 de [09](09-candidatos-a-eliminar.md)). Solo sobrevive `control-hero-a-titular` + helpers (LIVE) ([03](03-home-control-fijos.md)).
- ✅ **GastosV2** (lista LIVE), alta de gasto/ingreso, ciclos cerrados en el propio tab, edición de gasto por hoja sobre el feed, categorías, notas. Pantalla de historial y modal de filtros **retirados el 2026-08-05**; search global = SKIP por decisión owner ([04](04-gastos-add-flows.md)).
- ✅ Asistente financiero **100% heurístico** (`control-signals.ts`: 2171 líneas, 43 builders, 4 personas); coach mode scaffold; AI Coach LLM ⏸️ DEFERRED ([05](05-insights-asistente-coach.md)).
- ✅ Settings completo + engagement: Logros (14 codes, realtime), Manifiesto Wrapped, Ediciones, tours, subscriptions-zombie. Billing 🟡 **MOCK** (RevenueCat en pausa) ([06](06-settings-engagement.md)).

**Backend** ([07](07-backend-servicios-db.md)): 33 tablas / ~30 RPCs / 12 crons. Patrón snapshot (`home_snapshot`, `gastos_snapshot`) colapsa N round-trips. `control-advisor` (edge) usa `claude-sonnet-4-6` pero **no está invocado desde el mobile** (el asistente es 100% heurístico). Rachas viven en DB (`user_streaks`).

**Lo que frena el launch** ([08](08-estado-vs-decisiones-pasadas.md)): Apple Developer Program sin comprar → bloquea submit, IAP, push iOS, widgets. Deuda crítica independiente de Apple: **vulnerabilidad RLS abierta en `expenses`** (cualquier miembro de familia puede editar/borrar gastos de otro). CI corre solo lint+typecheck (los ~54 tests Vitest + 4 Playwright no corren en CI).
