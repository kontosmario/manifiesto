# Asistente — matriz de hardening por señal (auto-extraída de la auditoría)

## control-signals-validity-audit
### buildStressWeek  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:526-567)  [high:0 med:1]
- **contrato:** Signal fires deterministically IF AND ONLY IF: (1) at least 3 fixed_expenses exist, (2) each has status !== 'completed', (3) next_due_on is parseable as ISO YYYY-MM-DD, (4) parseFixedExpenseDate returns valid Date (interprets as local-day midnight, not UTC), (5) dueDate >= now AND dueDate <= now+7 days. Gate at L541 prevents firing with < 3 fijos.
- **guard:** Add `f.amount != null && f.amount > 0` check before summing (L542) to prevent $0 signals. Alternatively: document that null amounts are data errors and acceptable failure mode.
### buildPaydayProximity  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:570-601)  [high:0 med:0]
- **contrato:** Signal fires IF (1) diasRestantes ∈ [2,14], (2) restanteMes > 0, (3) sustainable = restanteMes/diasRestantes < cupoDiario×0.7. Division always safe: denominator ∈ [2,14].
- **guard:** Already well-guarded. Gate chain at L573-577 is exhaustive; no division risks.
### buildEndOfCycleAcceleration  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:604-640)  [high:0 med:0]
- **contrato:** Fires IF (1) detalleDias.length >= 6, (2) diasRestantes <= 5, (3) promedioDiario > 0, (4) last3Avg/cycleAvg >= 1.3. Confidence = rampOneCycle(detalleDias.length) ∈ [0,1]. No division by zero: gates at L607-612 ensure numerator/denominator validity.
- **guard:** Signal is well-guarded. Gates exhaustive. Assume detalleDias is sorted chronologically (most recent last); document if not.
### buildRecoveryPath  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:643-701)  [high:1 med:0]
- **contrato:** Fires IF (1) libreHoy < 0, (2) diasRestantes > 1, (3) newCupo = cupoDiario - (overspend/diasRestantes). Division safe: diasRestantes ∈ [2,∞). Returns recovery-hard if newCupo < cupoDiario*0.4, else recovery-soft.
- **guard:** Add guard: clamp newCupo to [0, cupoDiario] before the 40% check to prevent negative/nonsensical caps.
### buildCycleStartProjection  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1735-1775)  [high:0 med:1]
- **contrato:** Fires IF (1) closedDays ∈ [1,2], (2) ingresoMes > 0, (3) libre = ingresoMes - fijosMes - goalNeed <= 0, (4) libreRatio = libre/ingresoMes < 0.25. No division by zero: ingresoMes checked > 0 at L1740.
- **guard:** At L1751, add minimum threshold (e.g., `if (libre < 1000) return null`) to avoid micro-budget noise. Or document that signal is early-cycle guidance, best-effort.
### buildForecastPaydayGap  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1784-1814)  [high:1 med:1]
- **contrato:** Fires IF (1) forecast exists, (2) diasRestantes > 1, (3) pessimistic.dailyAvg > 0 AND finite, (4) totalProjected > restanteMes, (5) daysToZero = floor(restanteMes/dailyAvg) < diasRestantes. Gate at L1790 ensures denominator > 0. Confidence = rampOneCycle(detalleDias.length).
- **guard:** Add: `if (!Number.isFinite(f.pessimistic.dailyAvg) || !Number.isFinite(f.pessimistic.totalProjected)) return null` before L1793 to block NaN/Infinity from erratic signals.

## control-signals validity audit
### buildVelocityWarning  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:704)  [high:3 med:2]
- **contrato:** Only emits when velocity exists and stress_level != 'calm'. Confidence ramps closedDays/14. No divisor zero: libreMes = cupoDiario × (diasRestantes + detalleDias.length), guarded by Math.max(0, ...). Over is signed; impactRaw clamps >=0. Copy branches on over > 0: if true shows delta, else 'impactMantener'.
- **guard:** (1) Check cupoDiario > 0; if zero, suppress or use 'sin presupuesto' framing. (2) Verify (diasRestantes + detalleDias.length) > 0; if zero, suppress. (3) Wrap forecast_close_amount in Number.isFinite() check; if false, suppress. (4) For restanteMes < 0, surface different signal (e.g. 'recovery-complete'). (5) Clamp momentumPct to [0, 100] range explicitly.
### buildPositiveForecast  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:759)  [high:1 med:2]
- **contrato:** Emits only when (1) alcanzaElMes=true (projection reaches month-end), (2) sobrantePresupuestadoMes >= cupoDiario×2. Proposed contribution floored to nearest 1000. If hasActiveGoal=true AND proposed > 0, offer quick-contribution action; else degrade to 'open-savings-goal'. Confidence ramps closedDays/14 (T1).
- **guard:** (1) Verify sobrantePresupuestadoMes is finite and >= 0 at line 764 (Number.isFinite() check). (2) If not finite, suppress signal or log. (3) Check goal completion: hasActiveGoal && proposed > 0 && goal.currentAmount < goal.goalAmount before offering quick-action. (4) Consider rounding proposed down to 5k (not 1k) for better granularity. (5) Add explicit clamp: proposed = Math.max(0, proposed) after all math. (6) Document that alcanzaElMes is structural, not cash-flow; test with ingresoMes = 0, fijosMes high.
### rampOneCycle  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:142)  [high:0 med:0]
- **contrato:** Confidence = 0 when closedDays <= 0. Confidence = 1 when closedDays >= 14. Linear ramp between. Assumes 14 closed days ≡ one full cycle (~2 weeks of 28-30 day month). For biweekly cycles, 14 days = 2 full cycles (clamped to 1.0). Gate: signals checking confidence < MIN_CONFIDENCE (0.4) drop if closedDays < 5.6 days (~6 days).
- **guard:** No changes needed. clamp01 helper is defensive. Confidence ramp is sound. Assumption (14 days = 1 cycle) documented at line 141. For non-standard cycle lengths, consider reading from PayCycle instead of hardcoding 14, but current behavior is safe and reasonable.
### buildCategoryAcceleration  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:806)  [high:1 med:3]
- **contrato:** Gate: summaries.length > 0 (no historical data = return null). Fetches top category by current spend. Matches against historicalAvg via avgCategoryFromSummaries(), which defensively parses category_breakdown (array or legacy Record). Accel threshold defaults to 1.4× but uses user P75 if baselines provided and user has ≥3 closed cycles. Confidence floors at 0.5 even with 1 prior summary because signal gates on historicalAvg > 0 (data exists). Spike detection uses 7-day window.
- **guard:** (1) Verify historicalAvg is finite (Number.isFinite()) before division. (2) Check topNow.amount is finite and >= 0. (3) Add clamp: impactRaw = Math.max(0, Math.round(delta)). (4) Add explicit coercion for spike: const spike: boolean = isCategorySpike(...). (5) Log or skip if category_breakdown parse fails. (6) If ratio = Infinity, suppress (category spiked from $0, not acceleration of existing trend).
### buildForecastPaydayGap  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1784)  [high:2 med:0]
- **contrato:** Emits only if: (1) forecast != null, (2) diasRestantes > 1, (3) pessimistic.dailyAvg > 0, (4) pessimistic.totalProjected > remaining, (5) daysToZero < diasRestantes, (6) gapDays > 0. Logic: if pessimistic.totalProjected > remaining, user runs out before cycle end. Divide remaining by dailyAvg to get days to depletion. Subtract from diasRestantes to get gap. Confidence ramps closedDays/14 (T1).
- **guard:** (1) Verify pessimistic.dailyAvg is finite and > 0 before division. (2) Verify remaining is finite and >= 0. If remaining < 0, suppress. (3) Clamp daysToZero: Math.max(0, Math.min(diasRestantes, floor(remaining / dailyAvg))). (4) Add assertion gapDays > 1 before surfacing (not just > 0). (5) Wrap impactRaw: Math.round(Math.max(pessimistic.dailyAvg, gapAmount)) to ensure visible delta. (6) Add comments in forecast-engine about NaN/Infinity invariants for dailyAvg.

## control-signals validity audit
### buildCategoryAcceleration  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:806)  [high:1 med:5]
- **contrato:** Fires only when: summaries.length > 0 AND ≥1 category in current cycle AND historicalAvg > 0 from summaries AND topNow.amount / historicalAvg >= accelThreshold. Confidence ramps with closedDays (min 14 for 1.0) and summaries (floored at 0.5 per line 859).
- **guard:** Gate: summaries.length >= 1 AND historicalAvg > 0 (already done). Add: (1) historicalAvg range check (reject > 1M or < -1000 for corruption); (2) topNow.amount range check (reject negative); (3) spike detection use consistent timezone (UTC via getTime()); (4) when category not found in any summary, degrade confidence to 0.3 (below MIN_CONFIDENCE) or require ≥2 cycle matches; (5) add income sanity gate: if cupoDiario < 1000, floor confidence to 0.2.
### buildCategoryCapBreaches  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:870)  [high:3 med:3]
- **contrato:** Fires only when: limits.length > 0 AND spent >= threshold (line 890). Confidence always 1.0 (line 923) — assumes limits are user-explicit and current cycle spend is known. impactRaw = breach ? spent - cap : 0, always >= 0. No historical ramp.
- **guard:** Gate: limits.length > 0 AND all limits have monthly_cap > 0 AND 0 < warning_threshold_pct <= 100. Add: (1) validation: spent range [0, max(spent)], reject NaN; (2) cap range: reject cap <= 0; (3) threshold sanity: reject warning_threshold_pct outside [1, 100]; (4) category existence check: if category not found, degrade signal or skip; (5) add mid-cycle scaling: urgency='media' if less than 50% of cycle remains and ratio(spent/cap) < 2.0; (6) dedup by category_id before returning array.
### buildCategoryDominance  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:936)  [high:1 med:0]
- **contrato:** Fires only when: byCategory.length >= 2 AND total > 0 AND share >= dominanceFloor (line 950). Confidence ramps with closedDays (line 971). Single-category cycles are gated (line 940).
- **guard:** Gate: byCategory.length >= 2 AND total > 0 (already done). Add: (1) Reject negative amounts in byCategory entries (validate at groupExpensesByCategory); (2) Save10 formula: if share > 0.9, cap save10 at top.amount * 0.05 (diminishing return on extreme concentration); (3) Confidence gate: if closedDays < 7, set confidence = min(rampOneCycle(closedDays), 0.3) to avoid firing on micro-cycles; (4) when catDominanceP75 is null and summaries.length < 3, explicitly degrade confidence by *0.7 to signal thin baseline.
### buildCategoryReductionWin  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:979)  [high:1 med:2]
- **contrato:** Fires only when: summaries.length > 0 AND ≥1 category in current cycle AND avgCategoryFromSummaries > 0 AND c.amount / avg <= 0.7 AND (avg - c.amount) >= minDelta. Confidence ramps with closedDays (floored 0.5) and summaries (floored 0.5).
- **guard:** Gate: summaries.length > 0 AND byCategory.length > 0 (already done). Add: (1) Validate ingresoMes >= 0 and use Math.max(1000, abs(ingresoMes)*0.005); (2) Reject negative amounts in both current and historical: if c.amount < 0 or avg < 0, skip; (3) avgCategoryFromSummaries already defensive (returns 0 on NaN or mismatch); (4) add pct range validation: if pct > 100, cap to 100 (ratio rounding); (5) when categories > 1 qualify, pick by largest absolute delta then by highest percentage reduction (stable tiebreak); (6) degrade confidence if bestWin.delta is < 3×minDelta (barely above floor): multiply confidence by 0.8.
### groupExpensesByCategory helper  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1875)  [high:0 med:2]
- **contrato:** Sums e.price for all expenses where !e.commitment_id, grouped by e.category_id. Falls back to i18n fallback for name/displayName if category not found.
- **guard:** Add validation: (1) reject expense.category_id if null/undefined, or use a catch-all 'Uncategorized' sentinel; (2) validate e.price >= 0 at entry; (3) if categories.length === 0, log warning; (4) don't return entries with amount === 0 (clean output).
### avgCategoryFromSummaries helper  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1937)  [high:2 med:0]
- **contrato:** Averages category totals where name matches. Returns 0 if no summaries or no matches. Defensive parser accepts array or Record; sums and divides by count.
- **guard:** Add validation: (1) Line 1950, validate Number(match.total ?? 0) is finite, reject NaN; (2) Line 1953, use ?? 0 explicitly instead of relying on implicit; (3) reject categoryName if empty or null at entry; (4) Log when count=0 (silent data loss).
### isCategorySpike helper  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1918)  [high:0 med:2]
- **contrato:** Partitions category's discretionary expenses into last-7-days and total. Returns last7 / total >= 0.7. Returns false if total === 0.
- **guard:** Add validation: (1) reject negative amounts in spike ratio (ignore refunds, or tag separately); (2) use consistent timezone for cutoff (convert now to start-of-day local, compute ms-based cutoff in local time, then compare); (3) if last7 + prior don't sum to total, log anomaly; (4) clamp ratio to [0, 1] before >= 0.7 check.
### rampOneCycle confidence helper  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:142)  [high:0 med:0]
- **contrato:** Returns clamp01(closedDays / 14). clamp01 ensures [0, 1] range, handles NaN.
- **guard:** No changes — already robust.
### MIN_CONFIDENCE filter  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:137,232)  [high:0 med:1]
- **contrato:** Line 232: filters out s.confidence < 0.4. Confidence must be [0, 1] (enforced by ramps).
- **guard:** Add validation: ensure confidence is finite and in [0, 1] before line 232 check. Reject any signal with NaN confidence explicitly.

## control-signals
### buildSmallLeaksInsight  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1043)  [high:0 med:2]
- **contrato:** Trustworthy when: (1) ≥10 small expenses to establish pattern, (2) detalleDias.length ≥ some minimum (implies non-trivial closed days), (3) cupoDiario > 0 (valid daily budget), (4) total computed as sum with coercion (price??0). Output absent otherwise.
- **guard:** GATE 1: Require cupoDiario > 1000 (sanity check for valid daily budget). GATE 2: If cupoDiario ≤ 1000, return null (dormant or invalid cycle). GATE 3: In pctOfCycle calculation, cap the denominator to avoid division near-zero: Math.max(10000, cupoDiario * detalleDias.length) ensures ratio stays bounded. GATE 4: Explicitly check total > 0 before emitting (handles all-null or all-zero prices). GATE 5: Clamp pctOfCycle to [0, 1] before formatting percentage, to avoid weird '10000%' outputs.
### buildNightImpulse  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1078)  [high:1 med:2]
- **contrato:** Trustworthy when: (1) ≥10 discretionary expenses to establish pattern (low count = high variance), (2) totalAmount > 0 (non-zero spend to compute ratio), (3) hour extraction from created_at valid (timezone-aware parsing). Output absent otherwise.
- **guard:** GATE 1: Validate created_at is a valid ISO datetime before extracting hour. Use try-catch or prevalidation. GATE 2: Add explicit isNaN checks on nightAmount and totalAmount before computing ratio. GATE 3: Add clamp on nightPct to [0, 100] after multiplication to handle any floating-point overflow. GATE 4: If timezone handling is unclear, document the assumption (UTC vs local) and enforce it in the data pipeline. GATE 5: Consider raising night-window threshold to 71% instead of 70% to avoid boundary jitter.
### buildWeeklyPattern  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1131)  [high:0 med:1]
- **contrato:** Trustworthy when: (1) porDowEnriched has 7 entries (Mon-Sun), (2) globalAvg > 0 (baseline exists), (3) cycleDays ≥ 7 (at least one week of data or projection), (4) peorDow and weekend buckets have .avg ≥ 0 (no NaN/negative). Output absent if globalAvg ≤ 0, porDowEnriched missing, or no pattern strong enough.
- **guard:** GATE 1: Add explicit check that detalleDias.length ≥ 7 or cycleDays ≥ 7 before computing monthlyOccurrences (one full week minimum to see weekly pattern). GATE 2: Add check that weekend bucket has .length ≥ 2 (both Sat and Sun with avg > 0) before using wkExtra; otherwise discard weekend candidate. GATE 3: Cap dowExtra and wkExtra to non-negative (clamp at 0) if floating-point ever produces tiny negatives. GATE 4: Add tolerance band around thresholds (5000 ± 100, 8000 ± 100) to reduce boundary chatter. GATE 5: Validate porDowEnriched.length ≥ 7 explicitly and all entries have finite .avg/.count/.ratio (no NaN).
### buildHighSingleExpense  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1540)  [high:0 med:1]
- **contrato:** Trustworthy when: (1) cupoDiario > 0 (valid budget), (2) todayExpenses is non-empty (has data for today), (3) max price is ≥ 30% of cupo (non-trivial). Output absent if today has no discretionary expenses or max < 30% of cupo.
- **guard:** GATE 1: Validate cupoDiario > 100 (minimum sane budget). GATE 2: Adjust threshold from 30% to 31% or use <= 30% to avoid boundary friction. GATE 3: Document timezone assumption for created_at parsing (UTC vs local). GATE 4: Clamp price to non-negative (max(0, Number(price ?? 0))) to handle corrupted data. GATE 5: Consider updating dataDays to include today if todayExpenses.length > 0: dataDays = (todayExpenses.length > 0 ? 1 : 0) + view.detalleDias.length.
### buildDuplicateMerchant  (/Users/mario/apps/manifiesto/mobile/features/insights/control-signals.ts:1586)  [high:0 med:0]
- **contrato:** Trustworthy when: (1) ≥2 recent (48h) discretionary expenses to compare, (2) description exists and non-empty (keyable), (3) prices are close (within 5% tolerance, lo > 0), (4) list.length ≥ 2 after grouping (duplicate pair or more). Output absent if <2 expenses, no description, or price spread > 5%.
- **guard:** GATE 1: Require lo > 100 (minimum meaningful price to avoid false positives on tiny expenses). GATE 2: Adjust tolerance from 5% to 5.05% (strictly <) or document as ±5% with clearness. GATE 3: Add epsilon margin in floating-point comparison: (hi - lo) > (lo * 0.05 + 1) (add 1 cent buffer). GATE 4: Ensure description.trim().toLowerCase() and validate non-empty after trim. GATE 5: Add check that list.length ≥ 2 at point of return (already done, but make explicit in guard). GATE 6: Document 48h window clearly (inclusive? exclusive? midnight local or UTC?).

## Control Signals Validity Audit
### buildFijosRatioHealth  (control-signals.ts:1234-1264)  [high:0 med:1]
- **contrato:** Fires only when: (1) ingresoMes > 0, (2) ratio (fijosMes/ingresoMes) >= 0.6. The 50% threshold is hardcoded as target. Confidence always 1.0. Requires clean income/fijos numeric values; no null/NaN handling.
- **guard:** (1) Validate ingresoMes > 0 before emit (already done line 1237). (2) Add assertion that fijosMes is numeric and >= 0; clamp to 0 if negative (accounting inconsistency). (3) Document that signal reflects snapshot state at view-build time — is not real-time if user adds a fixed expense mid-cycle. (4) If stale-fijosMes is concern, rebuild view on fixed-expense mutation before calling buildControlSignals.
### buildIncomeVolatility  (control-signals.ts:1267-1312)  [high:0 med:1]
- **contrato:** Fires when: (1) summaries.length >= 2, (2) ingresoMes > 0, (3) historicalAvg > 0 (computed from summaries[0:3]), (4) |delta/historicalAvg| >= 10%. Confidence = rampSummaries(summaries.length), which is min(1, count/3). Requires clean numeric income values in summaries.
- **guard:** (1) Add a check: if diasRestantes > 5 (not late in cycle), clamp confidence to 0.5 or suppress entirely. Rationale: mid-cycle income is incomplete; wait until ~3 days before close. (2) Validate that each summary.monthly_income >= 0 before averaging; skip entries with negative values (they indicate refunds, not income). (3) Document in copy that signal reflects 'recorded income so far' — users near payday should not see a false 'income missing' alarm.
### buildSubUsageCheckin  (control-signals.ts:1321-1391)  [high:1 med:3]
- **contrato:** Fires per subscription when: (1) subscriptionCheckins.length > 0, (2) !c.hasOpenCancelIntent (user not already canceling), (3) scoreSubscriptionUsage(c, now).shouldAsk = true (cadence gate). Confidence always 1.0. Output capped at 2 signals (line 1388). Each task has impactScope='monthly', impactRaw=subscription.amount.
- **guard:** (1) Add type-safe guard: if (!score || typeof score.shouldAsk !== 'boolean') skip this checkin. (2) Validate subscription.amount > 0 before emitting; zero-cost items should not surface. (3) Validate c.name is non-empty string; use fallback 'Suscripción' if null. (4) Add try-catch around scoreSubscriptionUsage call; log error and return [] on failure. (5) Document that SubscriptionCheckin type must include all required fields (flag, shouldAsk, prompt); validate in adapter before threading to builder.
### buildFromPriceHikeNotifications  (control-signals.ts:1394-1441)  [high:2 med:0]
- **contrato:** Fires per hike when: (1) notification.kind = 'price_hike', (2) created_at >= now - 7 days, (3) NOT already dismissed at this price. Confidence always 1.0. Output capped at 2 (line 1412). Each task has impactScope='cycle', impactRaw=delta, with amounts extracted from metadata.
- **guard:** (1) Add numeric validation before line 1414: prev = Math.max(0, Number(n.metadata.previous_amount ?? 0)); next = Math.max(0, Number(n.metadata.new_amount ?? 0)). Clamp to prevent NaN. (2) If prev = 0, set pct = 100 (avoid Infinity), and use special copy variant 'Nuevo precio' instead of '% increase'. (3) Validate name is non-empty; use 'Suscripción' fallback if missing. (4) Assert delta >= 0 in output (if new < prev, it's a discount, which is rare; document behavior). (5) Consider validating that fixed_expense_id matches an actual fixed_expense in the current account (prevent orphaned alerts).
### buildIncomeMissing  (control-signals.ts:1703-1732)  [high:0 med:2]
- **contrato:** Fires ONLY when args.paydayPending = true. Confidence always 1.0. No data-driven gates beyond paydayPending. The signal is a safety valve to prevent downstream calculations (projections, stress tests) from assuming a cycle with incomplete income. Requires clean ingresoRecurrente/ingresoMes numerics.
- **guard:** (1) Validate paydayPending is boolean; default to false if undefined. (2) Pre-validate ingresoRecurrente >= 0; if negative, treat as 0 or exclude from display. (3) Document in copy that 'Confirmar cobro' refers to the *expected recurring salary*, not total income — bonuses/transfers are separate. (4) Consider adding a field to args: paymentReceivedAt (timestamp when income posted) — use that to auto-clear paydayPending rather than relying on user action. (5) For multi-payday users, thread all expected paydays to builder; fire signal only if *all* are unconfirmed.

## signal-validity-audit
### buildSavingsFeasibility  (mobile/features/insights/control-signals.ts:1446)  [high:2 med:2]
- **contrato:** Signal fires ONLY when: (1) goal exists AND isActive==true, (2) missing = goalAmount - currentAmount > 0, (3) targetMonths > 0 (not null, not ≤0), (4) monthlyNeeded = missing / months is calculable without dividing by 0 or negative, (5) monthlyActual (vault) is a real non-negative number from the control view. The vault value is capped against restanteMes so it cannot exceed the true remaining money; it is thus always 0 ≤ vault ≤ realRemaining. Monthly comparison is valid ONLY within a single cycle — NOT repeated monthly (each cycle is independent). Confidence ramps on closedDays/14; fires with confidence >= rampOneCycle(closedDays).
- **guard:** GUARD 1: Before gate on targetMonths, validate that goalAmount and currentAmount are both finite non-negative numbers: `if (!Number.isFinite(goal.goalAmount) || !Number.isFinite(goal.currentAmount) || goal.currentAmount < 0 || goal.goalAmount < 0) return null`. GUARD 2: After calculating missing, add `if (missing !== missing) return null` to catch NaN. GUARD 3: Validate vault: `if (!Number.isFinite(monthlyActual) || monthlyActual < 0) return null`. GUARD 4: Add an optional confidence floor: `confidence: rampOneCycle(args.view.detalleDias.length) * clamp01(diasConGasto / 7)` to require at least some spending days for a vault estimate to be reliable. GUARD 5: Document that the signal is INTRA-CYCLE only and does not forecast full-month behavior (user must check `sobrantePresupuestadoMes` for end-of-cycle sufficiency).
### buildSavingsOverachievement  (mobile/features/insights/control-signals.ts:1485)  [high:1 med:3]
- **contrato:** Signal fires ONLY when: (1) goal exists AND isActive==true, (2) targetMonths > 0 (not null, not ≤0), (3) missing = max(0, goalAmount - currentAmount) > 0 (goal not yet reached), (4) planned = missing / targetMonths is valid division, (5) actual = vault ≥ planned * 1.15 (user must be >15% ahead of plan to fire), (6) newMonths = max(1, round(missing / actual)) is calculable, (7) saved = targetMonths - newMonths ≥ 1 (at least 1 month of acceleration). Critical: vault is a CYCLE SNAPSHOT, not a monthly average — the 1.15× threshold accounts for variance.
- **guard:** GUARD 1: Validate goal.goalAmount and goal.currentAmount are finite non-negative before any math: `if (!Number.isFinite(goal.goalAmount) || !Number.isFinite(goal.currentAmount) || goal.currentAmount < 0 || goal.goalAmount < 0) return null`. GUARD 2: Validate vault: `if (!Number.isFinite(monthlyActual) || monthlyActual < 0) return null`. GUARD 3: Add assertion that saved >= 1 is checked (line 1499 does this, good). GUARD 4: Document in the body text that the 'early months' forecast assumes the current saving pace continues, and only fire when closedDays >= 7 (one full week): `confidence: rampOneCycle(args.view.detalleDias.length) * clamp01(diasConGasto / 7)` — this prevents a week-1 spike from claiming 'you'll finish 6 months early'. GUARD 5: Consider capping the newMonths result to avoid absurdly small values: `const newMonths = Math.max(1, Math.round(Math.max(1, missing / Math.max(1, actual))))`
### buildSavingsMilestone  (mobile/features/insights/control-signals.ts:1671)  [high:2 med:1]
- **contrato:** Signal fires if and ONLY if: (1) goal exists AND isActive==true, (2) currentAmount >= goalAmount (threshold is exact ≥, not >). The signal represents a factual milestone, not a prediction — currentAmount is the real balance from the database. Confidence is hardcoded 1.0 because there is no uncertainty: either the account balance satisfies the condition or it does not. This is a Type-0 (real-time) signal.
- **guard:** GUARD 1: Validate that goalAmount is finite, positive, and non-Infinity: `if (!Number.isFinite(goal.goalAmount) || goal.goalAmount <= 0 || !Number.isFinite(goal.currentAmount) || goal.currentAmount < 0) return null`. GUARD 2: The signal is already hardcoded confidence=1.0, which is correct (Type-0). No change needed. GUARD 3: Document that this signal is AUTOMATIC and happens when the database record reflects currentAmount >= goalAmount — it does NOT require user action (unlike some signals that wait for confirmation). GUARD 4: Consider adding a flag to prevent duplicate firing on the same goal across cycles: store a "milestone_seen_at" timestamp (already exists in the DB schema as line 92 `wrapped_seen_at` for similar purpose). GUARD 5: If the signal is shown to users via a replay or historical view (not just on the cycle it fires), add a check: `if (goal.currentAmount >= goal.goalAmount) return task; else return null;` — do NOT rely on cached currentAmount from prior cycles.

## Signal Validity Audit: control-signals.ts (1977 LOC)
### buildDataGapWarning  (1641-1668)  [high:0 med:0]
- **contrato:** Valid when: (1) expenses array is non-empty, (2) daysSince can be computed (now > lastTs), (3) daysSince is integer 3-14. Data absence itself is a valid signal.
- **guard:** Current guards are adequate. Ensure `expenses` is pre-sorted by `created_at desc` at the call site (verified in adapter). The impactRaw=0 and confidence=1.0 are correct (not data-starved, pure time signal).
### buildStreakEncouragement  (1818-1840)  [high:0 med:3]
- **contrato:** Valid when: (1) racha >= 3 (established streak, not noise), (2) sobrantePresupuestadoMes can be negative (still show streak), (3) cupoDiario > 0 (needed for impact copy). Racha is pure count; no temporal checks needed.
- **guard:** Add gate: if (args.cupoDiario <= 0) return null. Adjust body copy to handle sobrantePresupuestadoMes < 0 (e.g., 'aprovecha estos días para recuperarte' instead of mentioning surplus when projected negative). Test on accounts with racha=0-2, racha=3, racha>=10.
### buildControlSignals orchestration (MIN_CONFIDENCE filter + ranking)  (165-265)  [high:1 med:0]
- **contrato:** Valid when: (1) every signal s has 0 <= s.confidence <= 1, (2) cycleDays = detalleDias.length + diasRestantes is >= 1, (3) filtered list has >=0 items, (4) diversity budget respects MAX_SUPER_SIGNALS=2, (5) final output is capped at 5 items. A signal below MIN_CONFIDENCE should NOT reach user.
- **guard:** CRITICAL: Line 233 filter needs to also check 'Number.isFinite(s.confidence)' before comparing to MIN_CONFIDENCE, to reject NaN/Infinity. Add: 'if (!Number.isFinite(s.confidence) || s.confidence < MIN_CONFIDENCE) return false'. Audit all builders to ensure they never emit confidence !== [0, 1].
### buildVelocityWarning  (703-752)  [high:1 med:3]
- **contrato:** Valid when: (1) args.velocity is non-null, (2) stress_level is one of the 4 valid values, (3) libreMes = cupoDiario * cycleDays can be computed without NaN, (4) forecast_close_amount >= 0, (5) momentum >= 0. Signal gates on stress_level !== 'calm' so only fires when velocity engine has flagged concern.
- **guard:** Add explicit check: if (!Number.isFinite(args.velocity.momentum) || args.velocity.momentum < 0) return null. Add default case in switch to return null or throw. Test with momentum={0, 0.5, 1.0, 1.5, NaN, Infinity}.
### buildPositiveForecast  (759-801)  [high:1 med:0]
- **contrato:** Valid when: (1) alcanzaElMes === true (cycle closes positive), (2) sobrantePresupuestadoMes >= cupoDiario * 2 (meaningful surplus), (3) savingsGoal is non-null for the goal-based branch, (4) proposed = Math.floor((sobra * 0.5) / 1000) * 1000 >= 0. The 50% split and 1k rounding are policy constants, not validity issues.
- **guard:** Add guard: if (!Number.isFinite(args.view.sobrantePresupuestadoMes)) return null. Optionally add diasRestantes > 0 check to avoid firing on cycle-end. Ensure savingsGoal.goalAmount and currentAmount are always non-negative when isActive=true.
### buildCategoryAcceleration  (806-867)  [high:0 med:0]
- **contrato:** Valid when: (1) summaries.length >= 1 (at least 1 prior cycle), (2) avgCategoryFromSummaries returns >= 0, (3) topNow.amount >= avgCategoryFromSummaries * accelThreshold to fire, (4) accelThreshold defaults to 1.4 or uses baselines P75 >= 1.4. Below these conditions signal correctly returns null.
- **guard:** Current guards are solid. Ensure avgCategoryFromSummaries never returns NaN (it returns 0 on parse failure, correct). Test with: (1) new category not in history, (2) category with 0 spend this cycle, (3) baselines=null.
### buildCategoryCapBreaches  (870-933)  [high:0 med:2]
- **contrato:** Valid when: (1) limits.length >= 0, (2) each limit.monthly_cap > 0, (3) each limit.warning_threshold_pct in [0, 100] (percentage), (4) byCategoryId sums correctly, (5) for each limit, either spent >= threshold (warning) or spent > cap (breach), with correct urgency and impact.
- **guard:** Add validation at entry: for (const limit of args.limits) { if (limit.monthly_cap <= 0 || limit.warning_threshold_pct < 0 || limit.warning_threshold_pct > 100) continue; } to skip invalid limits silently.
### buildHighSingleExpense  (1540-1583)  [high:0 med:0]
- **contrato:** Valid when: (1) cupoDiario > 0, (2) todayExpenses correctly filtered to today (midnight boundary respected), (3) max expense found >= cupoDiario * 0.3, (4) price field is non-negative number.
- **guard:** Current implementation is solid. No changes needed. This is a T0 (real-time) signal and confidence=1.0 is correct.
### buildWeeklyPattern (DoW + Weekend variant)  (1131-1229)  [high:0 med:1]
- **contrato:** Valid when: (1) porDowEnriched has 7 entries (one per DoW), (2) peorDow.ratio >= 1.4 for DoW candidate to fire, (3) globalAvg > 0, (4) wkRatio >= 1.5 for weekend candidate, (5) dowExtra >= 5000 OR wkExtra >= 8000 for signal to fire. If neither threshold met, returns null.
- **guard:** Add explicit null/NaN checks for peorDow.avg and weekendAvgValue/wkdayAvgValue before using in division. Add validation that porDowEnriched.length === 7 at entry. Consider erring on dowIndexFromName instead of defaulting to 0.
### rampOneCycle confidence helper  (142-144)  [high:0 med:0]
- **contrato:** Valid when: (1) closedDays >= 0, (2) closedDays is finite number. Result is always in [0, 1].
- **guard:** Current implementation is solid. clamp01 already handles NaN/Infinity correctly. No changes needed.
### MIN_CONFIDENCE filter (0.4 threshold)  (136-137, 232-241)  [high:1 med:0]
- **contrato:** Valid when: (1) every signal s.confidence is in [0, 1] (or NaN/Infinity for edge cases), (2) blockedFamilies is a Set or null. Filtered list must contain only signals >= MIN_CONFIDENCE AND not in blockedFamilies.
- **guard:** CRITICAL FIX: Change line 233 to 'if (!Number.isFinite(s.confidence) || s.confidence < MIN_CONFIDENCE) return false' to reject NaN/Infinity before comparing.

## finance_assistant_signals_validity_audit
### rampOneCycle  (control-signals.ts:142-144)  [high:0 med:2]
- **contrato:** Valid when closedDays ≥ 1 and ≤ 90; outputs deterministic 0-1 range; clamp01() guards all non-finite inputs to 0
- **guard:** None needed—clamp01() already guards. However, signals using rampOneCycle should document minimum viable cycle length (7-14 days) in copy, not fire on < 3 closed days without explicit warning that confidence is low.
### computeRobustDailyAverage  (robust-daily-average.ts:45-105)  [high:2 med:2]
- **contrato:** Valid for ≥ 5 days; below that falls back to plain mean with hadEnoughData=false. Median threshold = max(median×3, 50k) strictly excludes (>) days. Requires all numbers to be finite.
- **guard:** Validate input: filter dailyTotals to only finite positive numbers before calling. Add guard: if(dailyTotals.length < 5) return { hadEnoughData: false, typicalAverage: 0, ... } immediately to signal cold-start rather than using plain mean. Document that < 5 days is unsafe for projection.
### buildForecast7Day baseline track  (forecast-engine.ts:162-175)  [high:2 med:3]
- **contrato:** Valid when promedioDiario > 0, diasRestantes ≥ 1, porDowEnriched non-empty. Horizon clamped to min(7, diasRestantes). Fixed dates parsed as local-day midnight (parseFixedExpenseDate).
- **guard:** Gate: if(baselineDailyAvg <= 0) return zero-filled Forecast7Day with explicit note 'insufficient_history'; do not extrapolate. Validate porDowEnriched is non-empty and has ≥1 bucket with avg>0 before using. Validate fixed expense amounts are positive and next_due_on is parseable ISO date.
### buildPositiveForecast  (control-signals.ts:759-801)  [high:0 med:2]
- **contrato:** Valid when alcanzaElMes=true AND sobrantePresupuestadoMes ≥ 2×cupoDiario. Otherwise returns null (no signal). Proposed contribution always ≥ 0 (max(0, floor(...))) and ≤ sobra/2.
- **guard:** Gate: if(!alcanzaElMes || sobra < 2×cupoDiario) return null. Gate: if(hasActiveGoal && currentAmount ≥ goalAmount) set hasActiveGoal=false before composing (goal already won). Validate: proposed ≥ 0 and log if proposed=0 with copytail explaining why.
### buildCategoryAcceleration  (control-signals.ts:806-867)  [high:0 med:1]
- **contrato:** Valid when summaries.length ≥ 1, byCategory.length ≥ 1, topNow.amount > 0, historicalAvg > 0, ratio ≥ accelThreshold. Confidence floor=0.5 when summaries=1 to avoid suppressing fully-closed first cycles.
- **guard:** None strictly needed—gates are defensive. However, document the confidence floor logic (0.5 minimum on summaries multiplier) in signal metadata so UI can explain 'early-cycle estimate'.
### buildCategoryCapBreaches  (control-signals.ts:870-933)  [high:1 med:3]
- **contrato:** Valid when limits.length ≥ 0 (empty array = []), categoryLimit.monthly_cap > 0, warning_threshold_pct ∈ [0, 100]. Each limit emits 0-1 signal.
- **guard:** Validate before emit: if(monthly_cap <= 0) skip this limit. If(warning_threshold_pct < 0 || warning_threshold_pct > 100) clamp to [0,100]. Remove duplicate category_ids upstream (dedupe on category_id).
### buildCategoryDominance  (control-signals.ts:936-976)  [high:0 med:2]
- **contrato:** Valid when byCategory.length ≥ 2, total > 0, top.amount > 0, share ≥ dominanceFloor (default 40%, user-calibrated ≥40%). Requires ≥3 prior cycles for custom baseline.
- **guard:** Validate: if(byCategory.length < 2 || total <= 0) return null. Enforce baselines?.catDominanceP75 ≥ 0.4 (clamp if < 0.4).
### buildFijosRatioHealth  (control-signals.ts:1233-1264)  [high:0 med:1]
- **contrato:** Valid when ingresoMes > 0 and ratio ≥ 0.6. Otherwise returns null. Impact = fijosMes - (ingresoMes × 0.5), must be ≥ 0.
- **guard:** Validate caller: ensure fijosMes ≥ 0 and ingresoMes > 0 before passing to buildControlSignals. Document 60% ratio as a survival threshold.
### buildIncomeVolatility  (control-signals.ts:1267-1312)  [high:0 med:0]
- **contrato:** Valid when summaries.length ≥ 2, ingresoMes > 0, historicalAvg > 0, |delta/historicalAvg| ≥ 10%
- **guard:** None strictly needed—gates are robust. Consider documenting that 10% threshold is fixed, not user-calibrated.
### buildForecasePaydayGap  (control-signals.ts:1784-1814)  [high:0 med:2]
- **contrato:** Valid when forecast exists, diasRestantes > 1, f.pessimistic.dailyAvg > 0, f.pessimistic.totalProjected > remaining. Gap must be > 0 days and < diasRestantes.
- **guard:** Validate: if(!forecast || !forecast.pessimistic) return null. Gate: if(f.pessimistic.dailyAvg <= 0) return null. Clamp gapAmount to reasonable bound (e.g., 10×monthly income) to prevent fantasy headlines.
### buildHighSingleExpense  (control-signals.ts:1540-1583)  [high:0 med:1]
- **contrato:** Valid when cupoDiario > 0, todayExpenses.length ≥ 1, max.price ≥ 0.3×cupoDiario
- **guard:** Validate: if(cupoDiario <= 0) return null. Review timezone handling in startOfLocalDay—ensure created_at is interpreted in user's local timezone, not UTC.
### buildDuplicateMerchant  (control-signals.ts:1586-1638)  [high:0 med:0]
- **contrato:** Valid when recent.length ≥ 2 after filtering; duplicate list ≥ 2, price variance ≤ 5%, lo > 0
- **guard:** None strictly needed—gates handle edge cases. Document that committed expenses are excluded (correct).
### buildSmallLeaksInsight  (control-signals.ts:1043-1075)  [high:2 med:0]
- **contrato:** Valid when small.length ≥ 10, pctOfCycle ≥ 12%, total > 0
- **guard:** Gate: if(cupoDiario <= 0 || detalleDias.length == 0) return null immediately. Require at least 3-5 closed days before firing (early cycles over-inflate the % with no spending history).
### buildNightImpulse  (control-signals.ts:1078-1121)  [high:1 med:0]
- **contrato:** Valid when discretionary.length ≥ 10, totalAmount > 0, nightPct ≥ 70%
- **guard:** CRITICAL: Fix timezone handling. Use user's local timezone when extracting hour, not UTC. Consider parsing created_at as local date and then extracting .getHours(), or use a date library with timezone support. Gate: confidence should require ≥ 21 days; do not fire on < 14 days.
### buildWeeklyPattern DoW premium  (control-signals.ts:1131-1229)  [high:0 med:2]
- **contrato:** Valid when porDowEnriched non-empty, globalAvg > 0, cycleDays = detalleDias.length + diasRestantes. Pattern needs ≥5k min impact (DoW) or ≥8k (weekend) to surface.
- **guard:** Validate: if(!porDowEnriched || porDowEnriched.length < 7) return null. Gate: require globalAvg > 0 and at least one DoW bucket with avg > 0. Check for duplicate DoW names upstream.
### composeMomentumImpact super-signal  (momentum-impact.ts:38-43)  [high:1 med:1]
- **contrato:** Valid when positive.impactRaw ≥ 0 and positive.impactScope ∈ {monthly, oneTime, cycle}. Defaults impactScope to 'oneTime' if missing.
- **guard:** Validate: if(!Number.isFinite(positive.impactRaw) || positive.impactRaw < 0) return { impactRaw: 0, impactScope: 'oneTime', label: '...' }. Require impactScope to be explicitly set in positive-forecast.
### computeUserBaselines per-user calibration  (user-baselines.ts:45-101)  [high:0 med:0]
- **contrato:** Valid when summaries.length ≥ 3; below that returns null for each metric. Each observation must have category_breakdown with total ≥ 0.
- **guard:** Validate: ensure summaries are properly sorted by period_start DESC (most recent first). Warn if closedCycles < 5; baselines from 3-4 cycles are fragile.
### detectFridayCascade causal engine  (causal-engine.ts:77-125)  [high:0 med:1]
- **contrato:** Valid when closedDays ≥ 21, discretionary.length ≥ 10, Saturdays observed ≥ 3, occurrences ≥ 4, confidence ≥ 0.4 (downstream gate)
- **guard:** Gate: if(occurrences < 4) return null. Validate: saturdayBaseline > 0 and all magnitude values > 0; if magnitude = 0, don't emit (no actual spike).

## financial-assistant-validity-audit
### stress-week (3+ fijos due in next 7 days)  (control-signals.ts:525-567)  [high:2 med:2]
- **contrato:** Fires only when ≥3 active (non-completed) fixed expenses exist with valid next_due_on dates falling in [now, now+7days]. Confidence is always 1.0 (real-time gate, no historical baseline). All fixtures must have positive amount > 0. Dates parsed defensively against UTC TZ pitfall.
- **guard:** 1. Validate amount > 0 before summing. 2. Require parseFixedExpenseDate() to succeed for due-date inclusion (guard null return). 3. Pre-flight check: if due.length < 3 in the cutoff window, return null. 4. Clamp final total to [0, ∞) to avoid NaN in UI. Test timezone handling in ART context.
### payday-proximity (sustainable daily spend too low)  (control-signals.ts:569-601)  [high:2 med:0]
- **contrato:** Requires diasRestantes ∈ [2,14] AND restanteMes > 0 AND (restanteMes / diasRestantes) < (cupoDiario × 0.7). Confidence=1.0 (real-time snapshot). Valid only when we have room for meaningful rebalancing (diasRestantes ≥ 2).
- **guard:** 1. Validate cupoDiario > 0 before any division. Clamp to [0.1, ∞) or return null. 2. Require restanteMes > 0 to trigger signal. 3. Ratio computation should clamp sustainable to [0, ∞). 4. Add explicit check: if sustainable is Infinity or NaN, return null. 5. Floating-point comparison should allow small epsilon (±0.001) tolerance.
### end-acceleration (last 3 days spent >130% of cycle average)  (control-signals.ts:603-640)  [high:1 med:0]
- **contrato:** Requires ≥6 closed days (need 3 for last-3, plus baseline context) AND diasRestantes ∈ [0,5] (end-of-cycle condition). promedioDiario must be > 0. last3Avg = (d[-3]+d[-2]+d[-1])/3. Ratio must be ≥1.3. Confidence = rampOneCycle(closedDays) = min(1, closedDays/14).
- **guard:** 1. Validate detalleDias is an array of non-null numbers before slicing. 2. Require all elements in last3 to be finite ≥ 0. 3. Guard promedioDiario > 0 explicitly, return null otherwise. 4. Compute last3Avg defensively; clamp to [0, ∞). 5. Check (last3Avg - cycleAvg) * 3 produces a finite non-negative extraSum before returning.
### recovery-path (overspending today, soft vs hard)  (control-signals.ts:642-701)  [high:2 med:0]
- **contrato:** Fires only when libreHoy < 0 (confirmed overspend, not pro-rated by hour) AND diasRestantes > 1 (need future days to recover over). Confidence=1.0. Hard variant triggers when newCupo < cupoDiario×0.4; soft otherwise. All numeric outputs clamp to [0, ∞).
- **guard:** 1. Validate cupoDiario > 0. If not, return null. 2. Require diasRestantes ≥ 2 (gating is strict enough). 3. Clamp newCupo to [0, cupoDiario] to prevent nonsense values. 4. Ensure overspend > 0 (redundant but defensive). 5. Guard persona ?? 'planner' fallback to ensure framingFor() never receives undefined.
### velocity-warning (forecast > budget by >15%)  (control-signals.ts:703-752)  [high:1 med:1]
- **contrato:** Requires velocity != null AND stress_level ∈ {calm|watch|warn|critical}. forecast_close_amount and momentum must be finite. libreMes = cupoDiario × (diasRestantes + detalleDias.length). If libreMes ≤ 0, over calculation is undefined but unlikely (cupoDiario should be > 0 when velocity exists). Confidence ramps over closedDays.
- **guard:** 1. Guard velocity != null and validate all fields are finite (forecast_close_amount, momentum). 2. Require stress_level ∈ {calm|watch|warn|critical}. If not, return null. 3. Ensure libreMes > 0 before over calculation (if ≤ 0, return null). 4. Clamp momentumPct to [0, ∞) (already done with max(0, ...)). 5. Clamp over to [0, ∞) before impactRaw assignment.
### positive-forecast (spare budget detected, propose savings)  (control-signals.ts:754-801)  [high:1 med:0]
- **contrato:** Requires alcanzaElMes=true AND sobra ≥ 2×cupoDiario. proposed = floor((sobra × 0.5) / 1000) × 1000 (round down to nearest 1k). If active goal exists, proposed=max(0, ...). Confidence ramps over closedDays. Valid only when projection is realistic (full cycle's worth of data).
- **guard:** 1. Require alcanzaElMes=true to proceed. 2. Guard sobra ≥ 2×cupoDiario. 3. Ensure sobra ≥ 0 before computing proposed (clamp to [0, ∞)). 4. Validate proposed is finite and ≥ 0 before returning. 5. Guard savingsGoal?.isActive with optional chaining; if null or not active, degrade to null-goal path. 6. Ensure goalTitle is a non-empty string if used in body.
### cat-accel (top category accelerating vs 4-week average)  (control-signals.ts:805-867)  [high:0 med:0]
- **contrato:** Requires ≥1 summary with category_breakdown containing the top category. historicalAvg must be > 0. Ratio = topNow.amount / historicalAvg must be ≥ accelThreshold. Spike detection: if ≥70% of category's cycle spend in last 7 days, framing changes to 'one-off' rather than 'trend'. Confidence floor at 0.5×rampSummaries prevents 0.33 collapse to below MIN_CONFIDENCE (0.4).
- **guard:** 1. Require summaries.length > 0 to proceed (explicit guard). 2. Validate avgCategoryFromSummaries returns a number ≥ 0. If 0, return null. 3. Ensure topNow.amount > 0 (should be; it's the max). 4. Guard isCategorySpike() never crashes on null/undefined prices; use Number(e.price ?? 0). 5. Confidence floor at 0.5×rampSummaries is correct — preserve it. 6. displayName fallback is safe; non-empty guaranteed.
### cat-dominance (one category >40% of discretionary)  (control-signals.ts:935-976)  [high:0 med:1]
- **contrato:** Requires ≥2 categories with spend (less meaningful to warn about 1 dominant category). Total > 0. share = top.amount / total must be ≥ dominanceFloor (0.4 default or user P75). All amounts must be ≥ 0.
- **guard:** 1. Validate byCategory.length ≥ 2. 2. Require total > 0. 3. Filter byCategory to only entries with amount > 0 before computing (exclude credits/refunds). 4. Ensure share ∈ [0, 1] or handle share > 1 gracefully. 5. save10 should clamp to [0, ∞). 6. displayName fallback to non-empty string.
### small-leaks (>10 small expenses, ≥12% of cycle budget)  (control-signals.ts:1042-1075)  [high:0 med:1]
- **contrato:** Requires small.length ≥ 10 AND (sum / (cupoDiario × closedDays)) ≥ 0.12. Both gates must pass. pctOfCycle calculation uses denominator max(1, cupoDiario × detalleDias.length) to avoid div-by-zero. Confidence ramps over closedDays.
- **guard:** 1. Filter expenses to (price != null && price > 0 && price < 5000) to exclude nulls and refunds. 2. Require small.length ≥ 10. 3. Require pctOfCycle ≥ 0.12. 4. Validate cupoDiario ≥ 0 (guard before denominator). 5. Ensure impact impactRaw = total × 0.3 is finite. 6. Clamp to [0, ∞).
### night-impulse (>70% of discretionary spend after 22:00)  (control-signals.ts:1077-1121)  [high:0 med:1]
- **contrato:** Requires discretionary.length ≥ 10. Night window: h ≥ 22 OR h < 2 (covers AR late dinner/night impulse without false-firing on normal dinners). nightPct = nightAmount / totalAmount ≥ 0.7. Confidence = rampThreeWeeks(closedDays) for pattern-level confidence.
- **guard:** 1. Require discretionary.length ≥ 10. 2. Ensure created_at parsing succeeds; if new Date() is Invalid, skip that expense. 3. Validate totalAmount > 0 before division. 4. Clamp nightPct to [0, 1] or validate before comparison. 5. Guard against timezone mismatches: if timestamps are UTC-encoded, convert to user's local time before getHours(). 6. Ensure nightAmount ≥ 0.
### weekly-pattern (worst DoW or weekend premium)  (control-signals.ts:1131-1229)  [high:0 med:1]
- **contrato:** Requires porDowEnriched.length = 7 AND globalAvg > 0. peorDow.avg > 0 AND ratio ≥ 1.4 to surface DoW. Weekend ratio ≥ 1.5 AND impact ≥ $8k to surface weekend. Final signal: one of {dow, weekend, none}. cycleDays used to scale monthly impact to actual cycle length (not hardcoded ×4 or ×8). Confidence = rampThreeWeeks(closedDays).
- **guard:** 1. Require porDowEnriched.length === 7. 2. Guard globalAvg > 0. 3. For DoW: require peorDow != null, peorDow.avg > 0, peorDow.ratio >= 1.4, dowExtra >= 5000. 4. For weekend: require weekday.length > 0, weekendAvgValue > 0, wkdayAvgValue > 0, wkRatio >= 1.5, wkExtra >= 8000. 5. dowIndexFromName should return [0..6] or null; map null to generic label. 6. Ensure cycleDays > 0 before division (should always be true).
### fijos-ratio (fixed >60% of income or >75%)  (control-signals.ts:1233-1264)  [high:0 med:0]
- **contrato:** Requires ingresoMes > 0. ratio = fijosMes / ingresoMes. Gate 1: ratio >= 0.6. Gate 2: severity switches at ratio > 0.75. All values must be finite ≥ 0.
- **guard:** 1. Require ingresoMes > 0. 2. Clamp ratio to [0, ∞) or validate fijosMes >= 0. 3. Ensure excess = fijosMes - target is finite. 4. Clamp impactRaw to [0, ∞). 5. Confidence=1.0 is correct (real-time snapshot).
### income-volatility (±10% variance from 3-month avg)  (control-signals.ts:1266-1312)  [high:0 med:0]
- **contrato:** Requires summaries.length ≥ 2. historicalAvg = sum(summaries[0:3].monthly_income) / min(3, summaries.length). If historicalAvg = 0, returns null. pct = (ingresoMes - historicalAvg) / historicalAvg. Gate: |pct| >= 0.1. Confidence = rampSummaries(summaries.length) = min(1, summaries.length / 3).
- **guard:** 1. Require summaries.length >= 2. 2. Validate historicalAvg > 0; if 0, return null. 3. Ensure all monthly_income values in sliced summaries are finite ≥ 0. 4. Clamp pct to [-∞, ∞) and round sensibly. 5. Use Math.abs(pct) ≥ 0.1 for comparison. 6. Clamp impactRaw to [0, ∞).
### robust-daily-average (outlier-resistant projection)  (robust-daily-average.ts:45-105)  [high:1 med:0]
- **contrato:** If dailyTotals.length = 0, returns all-zero result. If < 5 days, returns plain mean + hadEnoughData=false. If >= 5 days, computes median, sets threshold=max(median×3, $50k), partitions into typical/outlier. If all days flagged as outliers, falls back to plain mean. Returns typicalAverage >= 0.
- **guard:** 1. Guard dailyTotals.length > 0. 2. Filter out NaN/Infinity before sorting (validate all are finite). 3. Ensure median calculation is safe; handle sorted.length = 0. 4. Validate threshold > 0 before comparisons. 5. If typicalCount = 0, mark hadEnoughData=false (not true). 6. Clamp typicalAverage to [0, ∞).
### forecast-7day (pessimistic/baseline/optimistic tracks)  (forecast-engine.ts:162-270)  [high:0 med:1]
- **contrato:** Each track clamped to [0, 7] horizon = min(7, diasRestantes). daily[] is 7-element array; values beyond horizon are 0. base function must return ≥ 0 for each day. fixed amounts summed by ISO day. pessimistic uses DoW avg × 1.2 (or baseline if DoW avg=0). endingBalance = remaining - totalProjected (can be negative).
- **guard:** 1. Guard promedioDiario ≥ 0. 2. Guard diasRestantes ≥ 0; horizon = min(7, diasRestantes). 3. For pessimistic track, validate bucket.avg ≥ 0 and use fallback if 0. 4. parseFixedExpenseDate must not return null for valid dates; skip any null. 5. Fixed amounts must be ≥ 0; clamp to [0, ∞). 6. Daily values must be ≥ 0; enforce with Math.max(0, ...). 7. endingBalance can be negative; don't clamp. 8. Validate inflectionDays array is non-null and sorted.
### causal-engine (Friday cascade, paired impulse, stress spending)  (causal-engine.ts:77-238)  [high:0 med:1]
- **contrato:** Friday-cascade: closedDays ≥ 21, discretionary ≥ 10, Saturdays ≥ 3, Friday→Saturday pattern ≥ 4 occurrences, spike ≥ 1.4×. Paired-impulse: closedDays ≥ 14, discretionary ≥ 12, same category ≥6 pairs within 3h, priceB ≥ 0.5×priceA. Stress: closedDays ≥ 14, discretionary ≥ 12, ≥3 stress days (≥4 tx), baseline ≥7 days, stressAvg ≥ 1.3×baseline.
- **guard:** 1. Validate closedDays ≥ 0. 2. Filter expenses to discretionary (commitment_id == null). 3. Friday-cascade: require saturdayBaseline > 0 (not zero). 4. Paired-impulse: ensure priceA, priceB > 0 before ratio comparison. 5. Stress-spending: require baseline > 0, stressAvg > 0. 6. All confidence values must be ∈ [0, 1]; clamp with Math.min(1, ...). 7. Validate avg() never returns NaN (check values.length > 0).
### user-baselines (P75 personalization for cat-dominance & cat-accel)  (user-baselines.ts:45-152)  [high:0 med:0]
- **contrato:** Requires summaries.length ≥ 3. For each summary, extract category_breakdown, compute top category share (dominanceObs) and per-category acceleration peak (accelObs). If dominanceObs.length < 3, return null for catDominanceP75. If accelObs.length < 3, return null for catAccelP75. P75 clamped to [0.4, ∞) and [1.4, ∞) respectively to prevent regression below hardcoded defaults.
- **guard:** 1. Require summaries.length >= MIN_CYCLES (3). 2. Validate normalizeBreakdownEntries never crashes on null/malformed breakdown. 3. Ensure all amounts in entries are ≥ 0. 4. For dominance: require total > 0 before dividing. 5. For accel: require baseline > 0 before ratio. 6. percentile() edge cases: handle values.length = 0 (return 0), values.length = 1 (return values[0]). 7. Clamp final P75 results to min floors (0.4 and 1.4).
### momentum-impact (super-savings-momentum headline sizing)  (momentum-impact.ts:38-43)  [high:2 med:0]
- **contrato:** impactRaw from positive-forecast must be ≥ 0 (projected surplus). impactScope should be 'oneTime' or missing (defaults to 'oneTime'). Output label must explicitly reference 'ciclo' or 'este mes', never 'anual' or '×12'.
- **guard:** 1. Validate positive != null. 2. Clamp impactRaw to [0, ∞); if NaN, return zero-impact. 3. Force impactScope='oneTime' (ignore input if provided). 4. Validate fmt() never produces NaN string; if so, return fallback '$0'. 5. Verify i18n label contains 'ciclo' or 'mes' (audit i18n keys).

## Finance Assistant Validity Audit
### cupoDiario (Daily budget cap)  (control-v2-adapter.ts:318)  [high:3 med:0]
- **contrato:** Valid only when: ingresoMes >= 0, fijosMes >= 0, ahorroMes >= 0, cupoDays >= 1. Falls back to cupoDays=1 via Math.max(1, ...). Division by 1+ guarantees result is always a number (never Infinity). Matches home-dashboard numerics via same helper (computeFixedExpenseCycleSummary).
- **guard:** Gate buildHighSingleExpense, buildVelocityWarning, buildPaydayProximity: all divide by cupoDiario must check (cupoDiario > 0) before using it. Guard is already present in buildHighSingleExpense:1543 (if cupoDiario <= 0 return null) — extend to all builders that use cupoDiario in percentage math. Ensure view.restanteMes / view.libreMes are also capped >= 0.
### promedioDiario (Average daily spend)  (control-v2-mock.ts:423 via robust-daily-average.ts)  [high:2 med:1]
- **contrato:** Valid when: closedDays > 0 (line 48-54 returns {typicalAverage:0} if empty). Robust algorithm requires >= 5 closed days (line 60, MIN_DAYS_FOR_ROBUST=5). Threshold = max(median*3, 50k) (line 71) — absolute floor avoids false positives on tiny-median days. When all days > threshold, falls back to plainMean (line 90-96).
- **guard:** Require closedDays >= 7 before promedioDiario is used in projection (hasReliableProjection gate at control-v2-mock.ts:476 already does this). Require promedioDiario > 0 before division (line 481 checks this). For early-cycle signals, gate on rampOneCycle(closedDays) confidence multiplier — signals fire with low confidence until 14 days of data land (rampOneCycle formula: closedDays/14). Surface outlierDaysExcluded in UI always to show what was dropped.
### buildCategoryAcceleration (acceleration signal)  (control-signals.ts:806–867)  [high:1 med:0]
- **contrato:** Valid when: summaries.length >= 1 (gate line 809), historicalAvg > 0 (gate line 814), byCategory.length >= 1 (implicit via Array.sort). Confidence = rampOneCycle(closedDays) * max(0.5, rampSummaries(summaries.length)) (line 857–859). Ramp floors at 0.5 when summaries.length=1 so a fully-closed first cycle doesn't get dropped below MIN_CONFIDENCE.
- **guard:** Add fallback: when historicalAvg=0 and byCategory is non-empty with real spend, look further back (all summaries) or default to a minimum baseline so new categories aren't silent. Minimum historical threshold = max(byCategory.amount * 0.5, 5k) so the signal can fire when a category goes from dormant to active. Gate on isCategorySpike to reduce false positives when the spike is a one-off. Floor the accelThreshold at 1.4 if baselines.catAccelP75 is null (already done line 818).
### buildVelocityWarning (velocity signal)  (control-signals.ts:703–752)  [high:0 med:1]
- **contrato:** Valid when: velocity != null (line 707), stress_level in {calm|watch|warn|critical} (line 709 gate). libreMes = cupoDiario * (diasRestantes + detalleDias.length) (line 717–719). over = forecast - libreMes (line 720). Division by diasMes implicit in cupoDiario formula — never zero. Confidence = rampOneCycle(closedDays) (line 747).
- **guard:** Require velocity snapshot to have non-null, finite forecast_close_amount before using (add explicit Number.isFinite check). Require stress_level in enum {calm|watch|warn|critical} before firing. Match stress_level exactly (not via mapping) to urgency. When momentum is undefined or NaN, default to 1.0. Clamp momentumPct to 0..999 to avoid absurdly large percentages in body copy.
### buildPaydayProximity (payday signal)  (control-signals.ts:569–601)  [high:1 med:0]
- **contrato:** Valid when: diasRestantes > 1 and diasRestantes <= 14 (line 573), restanteMes > 0 (line 574), cupoDiario > 0 (implicit in line 577 division). sustainable = restanteMes / diasRestantes (line 576). Line 577 gates on (sustainable >= cupoDiario * 0.7) so only fires when per-day budget is tight.
- **guard:** Guard line 577: add explicit check (if (cupoDiario <= 0) return null) before division. Clamp impactRaw to 0 at line 592 (use Math.max(0, ...)) because negative impact is nonsensical for a warning signal. The 0.7 and 0.5 thresholds are tuned; document them as product constants at top of file.
### buildRecoveryPath (recovery signal)  (control-signals.ts:642–701)  [high:1 med:1]
- **contrato:** Valid when: libreHoy < 0 (line 653), diasRestantes > 1 (line 654), cupoDiario > 0 (implicit in line 656 and 657 division). newCupo = cupoDiario - (overspend / diasRestantes). Line 657 compares newCupo to 40% threshold. Confidence = 1.0 (real-time signal).
- **guard:** Require cupoDiario > 0 before division at line 656. Clamp newCupo >= 0 before storing (use Math.max(0, ...)). If newCupo ends up negative, treat as recovery-hard (user's situation is unrecoverable by end of cycle; recommend immediate action). Add a signal cap so recovery doesn't fire multiple times per cycle (use a dismiss ID that persists per cycle, similar to fijos-ratio).
### buildCategoryDominance (dominance signal)  (control-signals.ts:935–976)  [high:0 med:0]
- **contrato:** Valid when: byCategory.length >= 2 (line 940, need at least 2 cats to call one 'dominant'), total > 0 (line 942 gate), share > dominanceFloor (line 950). Confidence = rampOneCycle(closedDays) (line 971).
- **guard:** Boundary check line 950: use (share >= dominanceFloor) to fire, matching product intent ('dominant' should include exactly at threshold). Document dominanceFloor as a tunable constant. When baselines.catDominanceP75 exists, use it; else use 0.4. Floor catDominanceP75 at 0.40 (line 93 in user-baselines.ts already does this).
### buildHighSingleExpense (high single expense signal)  (control-signals.ts:1539–1583)  [high:0 med:0]
- **contrato:** Valid when: cupoDiario > 0 (line 1543 gate), todayExpenses.length > 0 (line 1554), price >= cupoDiario * 0.3 (line 1558). Confidence = 1.0 (real-time).
- **guard:** Guard at line 1543 is correct. For edge-case robustness: filter out expenses with price <= 0 or NaN before max() at line 1556 (though negative prices shouldn't exist in data). Clamp threshold at 20% (currently 30%) to catch more spending anomalies early.
### buildForecastPaydayGap (forecast survival signal)  (control-signals.ts:1783–1814)  [high:0 med:1]
- **contrato:** Valid when: forecast != null (line 1787), diasRestantes > 1 (line 1789), pessimistic.dailyAvg > 0 (line 1790), totalProjected > remaining (line 1792). daysToZero = restanteMes / pessimistic.dailyAvg (line 1793), gapDays = diasRestantes - daysToZero (line 1795), gapDays > 0 (line 1796 gate).
- **guard:** Require gapDays >= 1 (already line 1796) and gapAmount >= 1000 (add new gate). Require pessimistic.dailyAvg > 0 (already line 1790). If totalProjected is NaN or Infinity, treat as no forecast (return null). The 1.2× multiplier in forecast-engine.ts line 195 is baked in; document as 'pessimistic overhead'.
### rampOneCycle confidence ramp  (control-signals.ts:141–144)  [high:0 med:0]
- **contrato:** Valid always. clamp01 at line 156–160 ensures 0 <= result <= 1. MIN_CONFIDENCE floor = 0.4 (line 137), so signals fire when ramp >= 0.4 ≡ closedDays >= 5.6 ≈ 6 days.
- **guard:** No changes needed. The ramp formula is sound and the clamp01 guard is correct. Document 14 days as the ceiling in product specs.
### MIN_CONFIDENCE floor (0.4)  (control-signals.ts:137, 233)  [high:0 med:0]
- **contrato:** Valid when: signal.confidence >= 0 and <= 1 (builders ensure this via ramping/clamping). Filtering at line 233: if (s.confidence < MIN_CONFIDENCE) return false.
- **guard:** Floor is correct at 0.4. When the user has very low confidence signals but they're the only option (e.g., brand-new account), consider a feature flag to show them with a 'limited data' disclaimer (similar to 'según N días'). No data validity issue here — confidence threshold is a UX / product decision, not a math bug.
### control-v2-adapter: lastCycleDays fallback  (control-v2-adapter.ts:184–192)  [high:0 med:0]
- **contrato:** Valid when: period_start and period_end parse correctly (regex line 145). Falls back to 30 when parsing fails or result <= 0.
- **guard:** Fallback = 30 is safe and conservative. When summaries are malformed consistently, log a warning so ops can investigate backend date serialization. No data validity risk — fallback is deterministic and prevents division by zero.
### avgCategoryFromSummaries  (control-signals.ts:1937–1961)  [high:1 med:0]
- **contrato:** Valid when: summaries.length >= 1 (returns 0 if empty). Handles both array and legacy Record shape via normalizer. Returns 0 when category not found or all amounts are null/undefined.
- **guard:** Normalize category names to lowercase or a canonical form in the adapter when building category_breakdown. Else, case-sensitive matching will miss legitimate categories (e.g., 'restaurantes' vs 'Restaurantes'). Test with real server data that mixes cases. Return 0 is safe but silent; consider logging when a category lookup fails after N attempts.
### composeMomentumImpact  (momentum-impact.ts:38–43)  [high:0 med:1]
- **contrato:** Valid when: positive.impactRaw >= 0. impactScope = positive.impactScope ?? 'oneTime' (line 40), defaults to 'oneTime'. label via i18n.t(..., {amount: fmt(impactRaw)}) (line 41).
- **guard:** Clamp impactRaw >= 0 is already done. Verify that positive.impactScope is always set by the caller (buildPositiveForecast line 784 sets it correctly). No changes needed.
